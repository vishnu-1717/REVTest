diff --git a/app/api/webhooks/zoom/route.ts b/app/api/webhooks/zoom/route.ts
--- a/app/api/webhooks/zoom/route.ts
+++ b/app/api/webhooks/zoom/route.ts
@@ -60,6 +60,7 @@
 
     // Handle recording.completed event
-    if (body.event === 'recording.completed') {
+    if (body.event === 'recording.completed' || body.event === 'meeting.participant_joined') {
       const payload = body.payload || {}
       const accountId = payload.account_id
       const object = payload.object || {}
@@ -141,6 +142,12 @@
         // Continue even if show rate update fails
       }
 
+      // For participant_joined, we are done after updating show rate
+      if (body.event === 'meeting.participant_joined') {
+         return NextResponse.json({ received: true })
+      }
+
       // Check if transcript is available
       const transcriptFile = recordingFiles.find((file: any) => 
         file.file_type === 'TRANSCRIPT' || file.file_extension === 'vtt'
diff --git a/components/analytics/ClickableMetricCard.tsx b/components/analytics/ClickableMetricCard.tsx
--- a/components/analytics/ClickableMetricCard.tsx
+++ b/components/analytics/ClickableMetricCard.tsx
@@ -9,16 +9,16 @@
 const statusClasses: Record<MetricStatus, string> = {
   neutral:
     'border border-border bg-card text-card-foreground shadow-sm hover:border-border/80',
-  success:
-    'border border-green-400 bg-green-50 text-green-900 shadow-sm hover:border-green-500',
-  warning:
-    'border border-amber-400 bg-amber-50 text-amber-900 shadow-sm hover:border-amber-500',
-  danger:
-    'border border-red-400 bg-red-50 text-red-900 shadow-sm hover:border-red-500'
+  success: // emerald/green
+    'border border-emerald-500/20 bg-emerald-500/10 text-emerald-700 dark:text-emerald-400 shadow-sm hover:border-emerald-500/40',
+  warning: // amber/yellow
+    'border border-amber-500/20 bg-amber-500/10 text-amber-700 dark:text-amber-400 shadow-sm hover:border-amber-500/40',
+  danger: // red
+    'border border-red-500/20 bg-red-500/10 text-red-700 dark:text-red-400 shadow-sm hover:border-red-500/40'
 }
 
 const valueClasses: Record<MetricStatus, string> = {
   neutral: 'text-foreground',
-  success: 'text-green-800',
-  warning: 'text-amber-800',
-  danger: 'text-red-800'
+  success: 'text-emerald-700 dark:text-emerald-400',
+  warning: 'text-amber-700 dark:text-amber-400',
+  danger: 'text-red-700 dark:text-red-400'
 }
 
diff --git a/scripts/test-ghl-marketplace.sh b/scripts/test-ghl-marketplace.sh
new file mode 100755
--- /dev/null
+++ b/scripts/test-ghl-marketplace.sh
@@ -0,0 +1,48 @@
+#!/bin/bash
+
+echo "🚀 Testing GHL Marketplace Webhook (Localhost)"
+echo ""
+
+# Generate a timestamp and signature (mock)
+# In a real scenario, you'd need the real secret to generate a valid signature
+# or disable verification temporarily in code if testing without secret.
+TIMESTAMP=$(date +%s)
+
+echo "1. Testing Appointment Created..."
+curl -X POST "http://localhost:3000/api/webhooks/ghl/marketplace" \
+  -H "Content-Type: application/json" \
+  -H "x-ghl-timestamp: $TIMESTAMP" \
+  -H "x-ghl-signature: mock_signature_for_local_testing" \
+  -d '{
+    "type": "appointment.created",
+    "locationId": "test_location_123",
+    "appointment": {
+        "id": "apt_test_001",
+        "title": "Test Appointment",
+        "startTime": "'$(date -v+1d +%Y-%m-%dT%H:%M:%S%z 2>/dev/null || date -d "+1 day" +%Y-%m-%dT%H:%M:%S%z)'",
+        "endTime": "'$(date -v+1d -v+30M +%Y-%m-%dT%H:%M:%S%z 2>/dev/null || date -d "+1 day 30 minutes" +%Y-%m-%dT%H:%M:%S%z)'",
+        "status": "booked",
+        "contact": {
+            "id": "cont_test_001",
+            "name": "Test Contact"
+        }
+    }
+  }' \
+  -w "\nResponse: %{http_code}\n"
+
+echo ""
+echo "2. Testing Appointment Updated..."
+curl -X POST "http://localhost:3000/api/webhooks/ghl/marketplace" \
+  -H "Content-Type: application/json" \
+  -H "x-ghl-timestamp: $TIMESTAMP" \
+  -H "x-ghl-signature: mock_signature_for_local_testing" \
+  -d '{
+    "type": "appointment.updated",
+    "locationId": "test_location_123",
+    "appointment": {
+        "id": "apt_test_001",
+        "title": "Test Appointment Updated",
+        "status": "confirmed"
+    }
+  }' \
+  -w "\nResponse: %{http_code}\n"
